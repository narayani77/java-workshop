public class word {

    public static void main(String[] args){
        String sentence ="this program shows how to splite a string we need to find the sentence";
        String[] sentences =text.split(regex="\\");
        int sentence count=text.length;
        String[] words =text .split(regrx);
        int wordcount = word.length;
        String[] letter = text.split(regrex=" ")
        int lettercount = letter.length ;
        System.out.println("no.of letters:"+letters);
    }

}
